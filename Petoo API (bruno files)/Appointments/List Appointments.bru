meta {
  name: List Appointments
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/appointments?enterprise-id={{enterprise_id}}
  body: none
  auth: inherit
}

params:query {
  enterprise-id: {{enterprise_id}}
}

settings {
  encodeUrl: true
}

docs {
  Lista todos os agendamentos de uma enterprise
}
