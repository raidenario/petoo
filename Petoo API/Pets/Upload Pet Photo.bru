meta {
  name: Upload Pet Photo
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/pets/{{pet_id}}/photo
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:multipart-form {
  file: @file(/path/to/pet-photo.jpg)
}

settings {
  encodeUrl: true
}

docs {
  Upload de foto para o pet. Vai salvar em uploads/pets/ e atualizar o campo photo-url.
  
  Substitua /path/to/pet-photo.jpg por uma imagem real do seu sistema.
}
