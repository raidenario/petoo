meta {
  name: Request OTP
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/auth/otp/request
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "phone": "+5511999998888"
  }
}

docs {
  Solicita um código OTP (One-Time Password) para o telefone informado.
  Em ambiente de desenvolvimento, o token é gerado e retornado no campo `debug`.
}
