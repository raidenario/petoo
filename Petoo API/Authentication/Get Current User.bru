meta {
  name: Get Current User
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
}

docs {
  Retorna informações do usuário autenticado. Requer token JWT válido.
}
