meta {
  name: Dev Platform Login
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/v1/auth/dev-platform-login
  body: none
  auth: inherit
}

script:post-response {
  if (res.getStatus() === 200) {
      const jsonData = res.getBody();
      if (jsonData.token) {
          bru.setEnvVar('auth_token', jsonData.token);
          bru.setVar('auth_token', jsonData.token);
          console.log('Token PLATFORM (Dev) salvo:', jsonData.token.substring(0, 20) + '...');
      }
  }
}

docs {
  ENDPOINT TEMPORÁRIO DE DESENVOLVIMENTO.
  
  Gera um token JWT com role 'PLATFORM' (Superusuário PETOO) instantaneamente, sem conferir senha.
  
  Use para criar novas Enterprises ou testar permissões de superusuário.
}
