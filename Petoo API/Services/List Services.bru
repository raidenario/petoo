meta {
  name: List Services
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/services?enterprise-id={{enterprise_id}}
  body: none
  auth: inherit
}

params:query {
  enterprise-id: {{enterprise_id}}
}

settings {
  encodeUrl: true
}

docs {
  Lista todos os serviços de uma enterprise
}
